# TCAD–ML 역설계 시스템 발표 구성 (8분)

- **기준 문서**: `TCAD_ML_결과보고서_대전야호`
- **팀 / 출품분야**: 대전야호 · 소자/공정 (김두형, 김상윤, 김정필, 박상헌, 이지연)
- **발표 시간**: 8분 00초 / 본편 11장 (표지 포함) + Appendix 9장
- **한 줄 메시지**: "TCAD를 대체하는 게 아니라, TCAD로 재검증할 가치가 높은 공정 후보를 빠르게 선별하는 설계 지원 도구"
- **폰트**: `reports/assets/Pretendard-*.ttf` · **그림**: `reports/img/*.svg` (팀 제작) + 없는 컷은 점선 박스 → 팀이 삽입

### 본편 타임라인

| # | 슬라이드 | 보고서 | 시간 | 누적 |
|---|----------|--------|------|------|
| 1 | 표지 | – | 0:15 | 0:15 |
| 2 | 서론 — 왜 이 주제인가 / 문제점 → 해결 방향 | 1장 | 0:40 | 0:55 |
| 3 | 기본 소자와 공정 (이미지 위주) | 2.1 | 0:20 | 1:15 |
| 4 | Long vs Short 분리 이유와 공정 차이 | 2.2·2.3 | 1:00 | 2:15 |
| 5 | 전체 Flow (그림 5) + 왜 Random Forest | 3장 | 0:50 | 3:05 |
| 6 | 데이터 구성 / Valid·Invalid | 4.1·4.2 | 0:30 | 3:35 |
| 7 | Forward test — X→Y 타당성과 80/20 flow | 4.4 | 0:40 | 4:15 |
| 8 | Inverse test — Y→X를 한 이유 + UI | 4.5 ① | 0:45 | 5:00 |
| 9 | SHAP를 한 이유 + 미흡 부분(Short) 분리 | 4.5 ② | 0:35 | 5:35 |
| 10 | 핵심 결과·표 요약 → 의의 (결론) | 5.1·6.1 | 2:00 | 7:35 |
| 11 | 향후 개선 | 6.2 | 0:25 | 8:00 |

### Appendix (질의응답·심화용)

| # | 슬라이드 | 보고서 |
|---|----------|--------|
| A1 | 전류·SS의 log₁₀ 학습 | 4.3 |
| A2 | Random Forest 동작 원리 (그림 3) | 3.1·3.2 |
| A2·5 | UI 기반 공정조건 탐색 flow (그림 8) | 3.5 |
| A3 | 평가 지표 정의 | 5.2 |
| A4 | Forward prediction 성능 상세 (표 17) | 5.3 |
| A5 | Long-channel inverse recommendation 상세 | 5.4 |
| A6 | Short-channel inverse recommendation 상세 | 5.5 |
| A7 | SHAP 기반 물리적 타당성 (표 22, 그림 13) | 5.6 |
| A8 | 결과 해석 및 적용 범위 | 5.7 |
| A9 | 예상 질의응답 | – |

---

# 본편

## PPT 1페이지 — 표지 (0:15)

**화면**
- 제목: TCAD–Machine Learning 기반 2차원 Planar NMOS 역설계 및 공정 레시피 추천 시스템
- 부제: TCAD 데이터 + Random Forest surrogate model — 공정 설계공간 탐색 및 최종 후보 재검증 프레임워크
- 팀 대전야호 · 소자/공정 · 발표자명

**발표 멘트**
> "안녕하세요, 대전야호 팀입니다. 저희는 원하는 소자 특성을 입력하면 그에 맞는 공정 레시피를 추천하고, 그 결과를 TCAD로 다시 검증하는 역설계 시스템을 만들었습니다."

---

## PPT 2페이지 — 서론 (0:40) [보고서 1장]

**화면 – 왜 이 주제인가**
- 반도체 소자 특성은 공정조건(Gate Length, implant Dose/Energy, 열처리 …)이 결정 → 특성을 확인하려면 **TCAD 시뮬레이션이 필요**
- 공정변수가 늘수록 조합이 기하급수로 증가 → 전 영역을 직접 계산하는 것은 시간·연산자원상 불가능

**화면 – 문제점 (2가지)**
- ① **방향 불일치**: TCAD는 "공정조건 → 특성"인데, 설계자는 반대로 "원하는 특성 → 공정조건"을 알고 싶어 함
- ② **탐색 비효율**: 기준점 없이 넓게 훑으면 TCAD 자원이 낭비됨

**화면 – 해결 방향**
- TCAD 결과를 학습한 **surrogate model**로 유망한 공정 후보만 빠르게 추림 → **최종 후보만 TCAD로 재검증**하는 역설계 프레임워크

**발표 멘트**
> "반도체 공정조건이 소자 특성을 결정하기 때문에, 특성을 확인하려면 TCAD 시뮬레이션을 돌려야 합니다. 그런데 공정변수가 늘면 조합이 기하급수로 늘어서 전부 계산하는 건 불가능합니다. 더 큰 문제는 방향입니다. TCAD는 공정조건에서 특성으로 가는데, 설계자는 반대로 원하는 특성에서 공정조건을 알고 싶어 합니다. 그래서 저희는 TCAD 결과를 학습한 예측 모델로 유망한 공정 후보만 빠르게 추려내고, 최종 후보만 TCAD로 다시 검증하는 역설계 시스템을 만들었습니다."

---

## PPT 3페이지 — 기본 소자와 공정 (0:20) [보고서 2.1]

**화면 – 이미지 위주**
- **[이미지 박스] 그림 1 · 2차원 Planar NMOS 공정 단계별 단면** (팀 삽입)
- 공정 순서 한 줄: p-substrate → gate oxide/poly → LDD(As) → Halo(B) → Nitride spacer → S/D(P) → Anneal → Al contact
- 예측 출력 4개: **Vth · SS** (Vd 0.05 V) / **Ion · Ioff** (Vd 1.0 V)

**발표 멘트**
> "대상 소자는 2차원 planar NMOS입니다. 게이트를 형성한 뒤 LDD, Halo, 스페이서, 소스·드레인 주입과 어닐링을 거칩니다. 저희가 예측하는 특성은 문턱전압 Vth, subthreshold swing, on 전류와 off 전류 네 가지입니다."

---

## PPT 4페이지 — Long vs Short 분리 이유와 공정 차이 (1:00) [보고서 2.2·2.3]

**화면 – 문제 (짧은 채널)**
- **그림 2 · 정상 vs 비정상 ID–VG / NetActive 분포** → `reports/img/그림2.svg`
- 100 nm 이하에서 SCE(Drain 전계가 Source barrier에 간섭) → 비정상 turn-on, Vth 추출 실패, SS 수천 mV/dec
- 원인: 초기 공정이 250 nm급 기준 → Gate Length만 축소해 물리적 scaling 부족

**화면 – 두 채널 길이의 공정 차이 (핵심)**

| 항목 | Long (180–1000 nm) | Short (65 / 90 nm) |
|------|--------------------|--------------------|
| Gate oxide | 기존 두께 유지 | **2.0 / 2.5 nm 로 박막화** |
| Halo implant | 불필요 | **Dose/Energy 를 변수로 추가** (채널 barrier 보강) |
| Annealing | 고정 | **입력변수로 포함** (thermal budget 민감) |
| 입력 공정변수 | 5 개 | 8 개 |

- → 입력변수 구성 자체가 달라 하나의 모델로 묶을 수 없음 → **Short / Long 독립 모델 bundle**

**발표 멘트**
> "게이트 길이를 100 nm 아래로 줄이자 문제가 생겼습니다. 짧은 채널에서는 드레인 전계가 소스 쪽 barrier까지 영향을 줘서, 게이트를 올려도 정상적으로 켜지지 않고 문턱전압 추출이 실패하거나 SS가 수천 mV/dec까지 치솟았습니다. 원래 공정이 250 nm급 기준이라 게이트 길이만 줄인 게 원인이었습니다. 그래서 짧은 채널에서는 세 가지를 바꿨습니다. 게이트 산화막을 2 나노 수준으로 얇게 하고, 채널 중앙 barrier를 잡기 위해 Halo 주입의 도즈와 에너지를 새 변수로 넣고, thermal budget에 민감해진 어닐링도 입력에 포함했습니다. 결과적으로 긴 채널은 입력변수 5개, 짧은 채널은 8개로 구성이 완전히 달라졌기 때문에, 하나로 묶지 않고 Short와 Long 모델을 따로 학습했습니다."

---

## PPT 5페이지 — 전체 Flow + 왜 Random Forest (0:50) [보고서 3장]

**화면 – 전체 흐름도**
- **그림 5 · TCAD 생성 → 역설계 추천 → TCAD 재검증 (6단계, 전폭)** → `reports/img/그림5.svg`
- 6단계: ① TCAD 데이터 ② Valid/Invalid 라벨 ③ RF surrogate 학습 ④ 후보 50만 개 ⑤ Score 최소 = Top 1 ⑥ TCAD 재검증 (→ 데이터 재편입)

**화면 – 왜 Random Forest**
- 데이터가 수치형 표 형식(5~8 변수), 약 7,800개, 변수마다 물리적 의미 명확 → 딥러닝은 복잡도·튜닝 부담 과함
- RF: 비선형·상호작용 학습 / 전처리 부담 낮음 / **SHAP·Feature Importance 해석 용이** / 모델당 트리 300개

**발표 멘트**
> "전체 흐름입니다. 먼저 TCAD로 공정조건과 특성 데이터를 만들고, 계산이 정상인지 Valid/Invalid 라벨을 답니다. 이 데이터로 공정을 넣으면 특성을 예측하는 Random Forest surrogate를 학습합니다. 사용자가 목표 스펙을 넣으면, 학습 범위 안에서 만든 후보 50만 개를 이 모델로 순식간에 평가해 목표에 가장 가까운 레시피를 뽑고, 최종적으로 TCAD에 다시 넣어 확인합니다. Random Forest를 고른 이유는, 데이터가 수천 개 규모의 표 형식이고 변수마다 물리적 의미가 뚜렷해서 딥러닝은 과했기 때문입니다. Random Forest는 비선형 관계를 잘 잡고, 전처리 부담이 적고, SHAP으로 변수 영향을 해석할 수 있습니다."

---

## PPT 6페이지 — 데이터 구성 / Valid·Invalid (0:30) [보고서 4.1·4.2]

**화면 – 데이터 규모 (표 13)**

| 구분 | 원시 행 | 공정 레시피 | Valid | Invalid |
|------|---------|-------------|-------|---------|
| Short | 10,144 | 5,072 | 3,317 | 1,755 |
| Long | 5,500 | 2,750 | 2,332 | 418 |

**화면 – Valid/Invalid 라벨링**
- **그림 4 · 학습(분류=전체 / 회귀=Valid만)·추천(P(valid)≥0.5) 구조** → `reports/img/그림4.svg`
- **Valid = 계산·추출이 정상** (성능이 좋다는 뜻이 아님) / **Invalid = solver fail·출력 누락**
- 분류기 = Valid+Invalid 전체 학습 / 회귀기 = Valid & 4출력 정상 레시피만

**발표 멘트**
> "학습 데이터는 Short 약 5천 개, Long 약 2,750개 레시피입니다. Sentaurus에서 계산이 실패했거나 출력이 누락된 레시피를 Invalid로, 정상 추출된 걸 Valid로 라벨했습니다. 주의할 점은 Valid가 성능이 좋다는 뜻이 아니라는 겁니다. SS가 크든 누설이 크든, 계산과 추출만 정상이면 Valid입니다. 분류기는 전체로, 회귀기는 Valid 레시피만으로 학습합니다."

---

## PPT 7페이지 — Forward test: X→Y 타당성 (0:40) [보고서 4.4]

**화면 – 방법 (flow)**
- **그림 11 · Forward test(상) vs Inverse test(하) 설계** → `reports/img/그림11.svg`
- 전체 레시피 **80% training / 20% hold-out test** (random_state = 42)
- 분류: 클래스 비율 유지 stratified split / 회귀: 4출력 완전한 Valid 레시피, 동일 index로 split

**화면 – 의미**
- 예측 모델 **자체**의 정확도 시험 — 역설계 성능을 직접 의미하지는 않음
- 후보 탐색에 쓸 surrogate가 공정–특성 경향을 학습했는지 확인하는 **기반 시험**
- 구체 수치(R²·MAE·log RMSE)는 **결론 슬라이드**에서 함께 제시

**발표 멘트**
> "모델을 두 가지로 검증했습니다. 첫 번째는 Forward test입니다. 데이터를 80대 20으로 나눠서, 20%는 학습에 전혀 쓰지 않고 예측 오차만 측정합니다. 분류기는 Valid·Invalid 비율을 유지하도록 나누고, 네 개 회귀기는 같은 테스트 레시피에서 평가합니다. 이 시험은 예측 모델 자체가 공정과 특성의 관계를 제대로 배웠는지 보는 기반 시험이고, 구체적인 수치는 뒤 결론에서 함께 보여드리겠습니다."

---

## PPT 8페이지 — Inverse test: Y→X를 한 이유 + UI (0:45) [보고서 4.5 ①]

**화면 – 왜 Inverse test인가 (우리의 목적)**
- Forward test는 "공정 → 특성"이 맞는지만 확인 → 사용자가 스펙을 넣고 받은 추천이 **실제 TCAD에서 달성되는지는 별도 검증 필요**
- 방법: 실제 설계 사양 기준 목표 **300건** → Top 1 추천 → Sentaurus 재입력 → ① 예측 vs 실제 오차 ② 최초 목표범위 충족 여부
- **그림 6 · 직접 Y→X 대신 X→Y surrogate + 후보 탐색** → `reports/img/그림6.svg`

**화면 – 사용자 UI**
- **[이미지 박스 크게] 그림 7 · 목표 스펙 입력 → 추천 공정조건·예상 특성 (Top 3)** → `reports/source/figures/ui_recommendation_screen.png` (실제 UI 스크린샷)

**발표 멘트**
> "두 번째가 저희의 진짜 목적에 해당하는 Inverse test입니다. 사용자가 UI에 목표 스펙을 넣으면 추천이 나오는데, 그 추천이 실제 TCAD에서도 달성되는지는 Forward test로는 알 수 없습니다. 그래서 실제 설계에서 요구되는 스펙 300건을 목표로 넣고, 나온 Top 1 레시피를 Sentaurus에 다시 입력해서, 모델 예측과 실제 결과의 오차, 그리고 처음 요청한 목표 범위에 들어오는지를 확인했습니다. 화면이 실제 UI인데, 목표를 입력하면 추천 공정조건과 예상 특성이 Top 3까지 나옵니다."

---

## PPT 9페이지 — SHAP를 한 이유 + 미흡 부분 분리 (0:35) [보고서 4.5 ②]

**화면 – 왜 SHAP인가**
- RF가 잘 맞혀도 "어떤 공정변수가 결과를 움직였는지" 모르면 물리 검토 불가
- SHAP으로 변수별 기여도·방향을 정량화 → ① 모델이 실제 소자물리를 학습했는지 검증 ② 추천이 목표와 어긋날 때 어떤 변수를 다시 볼지 안내
- **[이미지 박스] 그림 9 / 그림 13 · SHAP 분석 UI 화면** (팀 삽입)

**화면 – 미흡한 부분 분리**
- **Long**: 네 출력 모두 안정 → 추천 도구로 활용 가능
- **Short**: SCE 경계에서 Vth·SS 오차 → subthreshold 지수 관계로 Ioff 배수오차 증폭(약 60배) → "정답"이 아니라 **추가 TCAD 검증이 필요한 후보를 좁히는 탐색·진단 도구**

**발표 멘트**
> "추천 결과는 SHAP으로 해석합니다. 모델이 잘 맞혀도 어떤 공정변수가 그 결과를 만들었는지 모르면 물리적으로 신뢰하기 어렵기 때문에, 변수별 기여도와 방향을 UI에서 함께 보여줍니다. 이걸로 모델이 실제 소자물리를 학습했는지 확인하고, 추천이 목표와 어긋날 때 어떤 변수를 다시 볼지 판단합니다. 그리고 여기서 시스템의 한계도 분리됩니다. 긴 채널은 네 출력 모두 안정적이라 추천 도구로 쓸 수 있지만, 짧은 채널은 SCE 경계에서 오차가 커지고 특히 누설전류가 크게 벌어져서, 정답을 주는 도구가 아니라 추가 검증이 필요한 후보를 좁히는 진단 도구로 씁니다."

---

## PPT 10페이지 — 핵심 결과·표 요약 → 의의 (2:00) [보고서 5.1·6.1]

**화면 – Forward prediction 성능 (표 17 요약)**

| 모델 | Vth | SS | Ion | Ioff | 분류기 |
|------|-----|-----|-----|------|--------|
| Long | R² 0.986 / MAE 6.20 mV | R² 0.994 / MAE 2.66 mV/dec | R² 0.994 | R² 0.794 | bal. acc 99.19% |
| Short | R² 0.960 / MAE 28.0 mV | R² 0.831 | R² 0.987 | R² 0.996 (log RMSE 큼) | bal. acc 98.35% |

**화면 – Inverse test 재검증 (표 15·18·20)**
- 총 **300개 요청 → 274개 고유 레시피 → TCAD 재계산 100% Valid**

| 모델 | Vth MAE | SS MAE | Ion 배수 | Ioff 배수 |
|------|---------|--------|----------|-----------|
| Long | 10.2 mV | 1.47 mV/dec | ×1.04 | ×1.92 |
| Short | 85.1 mV (중앙값 43.9) | 23.2 mV/dec (중앙값 7.1) | ×1.33 | ×61 (중앙값 ×19.8) |

- 목표범위 만족률 — Long: Vth 98.5 / SS 100 / Ion 100 / Ioff 76 % · Short: 53 / 84 / 90 / 17 %

**화면 – 의의**
- TCAD를 대체하는 게 아니라, **넓은 공정공간에서 재검증 가치가 높은 후보를 빠르게 선별**하는 설계 지원 도구
- **Long = 추천 도구 / Short = 탐색·진단 도구**
- 추천 → TCAD 재검증 → 검증 데이터 재편입 반복 → 신뢰성·적용 범위 점진 확대
- 제한된 TCAD 자원을 목표 달성 가능성이 높은 후보에 집중

**발표 멘트**
> "결론입니다. 먼저 Forward test에서, Long 모델은 네 출력 모두 R²가 0.98 이상으로 안정적이고 문턱전압 오차는 평균 6 밀리볼트였습니다. Short는 Vth와 on·off 전류는 잘 맞았지만 subthreshold 쪽에서 오차가 남았습니다. 분류기 정확도는 두 모델 모두 98퍼센트를 넘습니다.
> 핵심은 Inverse test입니다. 목표 300건에서 나온 274개 고유 레시피를 TCAD에 다시 넣었더니 전부 정상 계산됐습니다. Long은 문턱전압 오차 10 밀리볼트, on-current는 거의 일치했고 목표 범위 만족률도 대부분 100퍼센트에 가깝습니다. Short는 on-current는 안정적이지만 누설전류가 크게 벌어졌습니다.
> 그래서 이 시스템의 의의는, TCAD를 대체하는 게 아니라 수많은 공정 후보 중에서 TCAD로 재검증할 가치가 높은 것을 빠르게 골라주는 데 있습니다. 검증된 데이터를 다시 학습에 넣으면서 반복할수록 신뢰성과 적용 범위가 넓어집니다."

---

## PPT 11페이지 — 향후 개선 (0:25) [보고서 6.2]

**화면 (표 23)**

| 우선순위 | 개선 항목 | 기대 효과 |
|----------|-----------|-----------|
| 1 | 65/90 nm 경계·급변 영역 중심 추가 DOE / active learning | 적은 TCAD 추가로 Short 오차 집중 감소 |
| 2 | Bias sweep 범위 확장 (0–2.5 V → 확대) | 고정 동작점 기준 일관 추출 |
| 3 | Long-channel에 Halo·gate oxide 두께 변수 추가 | 안정성 유지하며 표현력 확장 |
| 4 | 트리 분산 / conformal prediction 기반 불확실성 표시 | Top 1 과신 방지, 재검증 우선순위 제공 |

**발표 멘트**
> "향후 개선입니다. 짧은 채널 경계 영역에 데이터를 집중 보강하고, bias sweep 범위를 넓히고, 긴 채널에도 Halo와 산화막 두께를 변수로 추가할 계획입니다. 또 Top 1 추천을 과신하지 않도록 불확실성 지표를 함께 제공하려 합니다. 데이터가 쌓일수록 정확도가 올라가는 구조라 발전 여지가 큽니다. 감사합니다."

---

# Appendix

## PPT A1 — 전류·SS의 log₁₀ 학습 [보고서 4.3]
- Ion(~10⁻⁴ A)·Ioff(~10⁻¹² A)를 linear로 함께 학습 → 큰 전류가 loss 지배, 작은 전류 오차 무시
- 예: 예측 10⁻¹² vs 실제 10⁻⁶ A → 6 decade 차이지만 linear squared error ≈ 10⁻¹²로 극소
- 대응: 전류는 log₁₀(|I|), SS도 극단값 완화 위해 log₁₀(SS) 학습 / Vth는 linear / UI 출력 시 10의 거듭제곱 복원

## PPT A2 — Random Forest 동작 원리 [보고서 3.1·3.2]
- **그림 3 · TCAD 공정변수로 구성한 Random Forest 개념도** → `reports/img/그림3.svg`
- 각 트리: bootstrap 표본 + node마다 "어떤 변수를 어떤 기준값으로 나누면 오차가 최대로 줄까" 반복 → leaf에 평균값 저장
- 최종 예측 = 300개 트리 예측값의 평균 (분류는 확률 종합)

## PPT A3 — 평가 지표 정의 [보고서 5.2]

| 지표 | 정의 · 해석 | 적용 |
|------|-------------|------|
| R² | 실제값 변동 중 모델이 설명한 비율 (정확도 %로 해석 안 함) | Vth·SS·log current |
| MAE / RMSE | 실제 단위 오차 / 큰 오차·tail에 민감 | Vth·SS |
| log MAE·RMSE | log₁₀ 공간 오차 (1 decade = 10배, 0.301 ≈ 2배) | Ion·Ioff |
| 목표범위 만족률 | TCAD actual이 min–max 안에 든 비율 (조건부 보조지표) | inverse suite |

## PPT A4 — Forward prediction 성능 상세 [보고서 5.3, 표 17]

| 모델 | Vth | SS | Ion | Ioff |
|------|-----|-----|-----|------|
| Short | R² 0.960 / MAE 28.0 mV | R² 0.831 / logRMSE 0.207 | R² 0.987 / logRMSE 0.609 | R² 0.996 / logRMSE 0.605 |
| Long | R² 0.986 / MAE 6.20 mV | R² 0.994 / MAE 2.66 | R² 0.994 / logRMSE 0.010 | R² 0.794 / logRMSE 0.267 |

- 분류기: balanced accuracy Short 98.35% / Long 99.19%, ROC-AUC 99.93% / 99.92%
- Short Ion·Ioff는 높은 R² + 큰 logRMSE 공존 → 경향은 학습, 경계 레시피 전류오차 잔존 → forward R²만으로 역설계 성능 과대평가 금지

## PPT A5 — Long-channel inverse recommendation 상세 [보고서 5.4]

| 출력 | 설명력 | 평균오차 |
|------|--------|----------|
| Vth | R² 0.935 | MAE 10.2 mV / RMSE 18.6 mV |
| SS | R² 0.685 | MAE 1.47 / RMSE 2.50 mV/dec |
| Ion | log R² 0.982 | log MAE 0.017 (≈1.04×) |
| Ioff | log R² 0.920 | log MAE 0.282 (≈1.92×) |

- 목표범위 만족률: Vth 98.5% / SS 100% / Ion 100% / Ioff 76% — 조건부 결과, test suite 한정
- 200개 요청 → 175개 고유 레시피 전부 정상 계산 → 전체 workflow 실현 가능성 입증

## PPT A6 — Short-channel inverse recommendation 상세 [보고서 5.5]

| 출력 | 중앙값 | 평균 · tail |
|------|--------|-------------|
| Vth | 43.9 mV | MAE 85.1 / RMSE 142.0 |
| SS | 7.1 | MAE 23.2 / RMSE 58.1 mV/dec |
| Ion | ≈1.27× | logMAE 0.125 (≈1.33×) |
| Ioff | ≈19.8× | logMAE 1.785 (≈61×) |

- |SS 오차| ↔ |Ioff log 오차| Spearman ρ = 0.586 (p<0.001) — subthreshold Id ∝ 10^((Vg−Vth)/SS)로 지수 증폭
- 목표범위 만족률: Vth 53% / SS 84% / Ion 90% / Ioff 17%

## PPT A7 — SHAP 기반 물리적 타당성 [보고서 5.6]

| 출력 | 지배 변수 (기여도) | 방향 | 대응 물리 |
|------|--------------------|------|-----------|
| Vth | Gate Length 32.1% | 짧을수록 Vth ↓ | Vth roll-off |
| SS | SD Energy 38.6% / Dose 38.4% | S/D가 gate control 지배 | subthreshold 누설 경로 |
| Ion | Gate Length 59.8% | 짧을수록 Ion ↑ | drive current 채널길이 의존 |
| Ioff | Gate Length 40.6% | 짧을수록 Ioff 급증 | SCE·DIBL 누설 |

- **[이미지 박스] 그림 13 · Long 모델 출력별 SHAP beeswarm 분포** (팀 삽입)
- 네 출력 모두 방향이 소자물리와 일관 → 물리적으로 의미 있는 관계 학습 (인과의 정량 크기 증명은 아님)

## PPT A8 — 결과 해석 및 적용 범위 [보고서 5.7]
- Forward = DOE 표본화 공정범위 내 interpolation 성능 / Inverse = 274개 고유 레시피 TCAD 재계산으로 예측–실제 오차 직접 측정
- 학습하지 않은 채널 길이·새 공정범위 extrapolation 해석 불가 → UI는 학습된 이산 gate length만 선택
- 목표범위 만족률·Valid probability·score는 TCAD 오차의 신뢰구간이 아님
- 출력의 정의: '정답 레시피'가 아니라 'TCAD로 재검증할 가치가 높은 후보 레시피'

## PPT A9 — 예상 질의응답

| 질문 | 답변 요지 |
|------|-----------|
| 왜 후보 50만 개인가 | 탐색 밀도와 응답시간의 절충. 더 늘려도 개선폭 감소·자원만 증가. seed 고정으로 재현성 확보 |
| 전역 최적해 보장? | 아님. '비교 가능한 여러 후보 중 최선'. 분류기·공정범위 제약·예상특성 제시가 장점 |
| Short 모델은 실패인가 | 용도가 다름. SCE 영역 진단·후보 선별용. Ioff 민감성은 물리적으로 예견됨 |
| 학습 안 한 gate length는 | extrapolation 보장 못 함 → UI에서 이산 gate length만 선택 제한 |
| 딥러닝이 더 낫지 않나 | 원리적 불가가 아니라, 현재 데이터 규모·형태에서 튜닝 부담 대비 이점이 작음 |
| Valid의 기준 | 데이터(계산·추출) 성공 여부. Long은 4출력 non-NaN, Short는 팀이 판정한 curve validity. 성능 임계값 아님 |

---

## 발표 팁
- **8분 엄수**: 4페이지(공정 차이)와 10페이지(결론)가 가장 길다 → 리허설로 두 장을 목표시간에 고정
- **반복 메시지**: "정답 레시피가 아니라, TCAD로 재검증할 가치가 높은 후보" — 2p·9p·10p
- **숫자는 2개**: Long Vth MAE 10.2 mV ↔ Short Ioff 약 61배 → 두 모델의 성격 차이
- **이미지**: 그림 2·3·4·5·6·8·11 = `reports/img/*.svg` (팀 제작). 그림 7 = UI 스크린샷. 그림 1·9·13 = 점선 박스 → 팀 삽입
